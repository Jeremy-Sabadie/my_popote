/**
 * Configuration utilisée lors du build de production.
 *
 * L'URL définitive de l'API Render sera renseignée au moment
 * du déploiement de My Popote.
 */
export const environment = {
  production: true,
  apiUrl: '',
};
